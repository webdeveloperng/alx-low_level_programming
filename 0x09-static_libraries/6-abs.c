#include "main.h"
/**
 * _abs - computes the absolute value of an integr
 * @n: the int to checks
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
